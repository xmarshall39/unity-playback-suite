using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace UPBS.UI
{
    public class PBSettingsTabGroup : PBTabGroup
    {
        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}
